#include <stdio.h>

int main()
{
    return 0;
}

/*
 * Queue is a linear data structure which follows a particular order in which the operations are performed.
 * Is an Abstract Data Type (ADT) that holds a collection of elements.
 * First in first out (FIFO).
 * 
 * Insertion (Enqueue) is performed from the Rear/fail
 * Deletion (Dequeue) is performed from the Front/head 
 * 
 * Applications:
 * - CPU scheduling
 * - Disk scheduling
 * - Printer queue
*/